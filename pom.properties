# Project Properties
project.name=Image Management Platform
project.description=Enterprise-grade image management platform with microservices architecture
project.version=1.0.0
project.organization=Synchrony Financial

# Build Properties
maven.build.timestamp.format=yyyy-MM-dd HH:mm:ss
build.timestamp=${maven.build.timestamp}

# Environment Properties
default.environment=dev
supported.environments=dev,test,staging,prod

# Docker Properties
docker.registry=synchrony
docker.image.prefix=image-management

# Quality Gates
minimum.code.coverage=80
sonar.exclusions=**/dto/**,**/config/**,**/Application.java